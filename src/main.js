/**
 * Entry point of the application.
 */
(async () => {
  console.log('Hello, World!')

  const logMany = (message, times) => {
    for (let i = 0; i < times; i++) {
      console.log(message)
    }
  }
})();
